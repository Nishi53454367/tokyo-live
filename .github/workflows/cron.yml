name: 5-minite-cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Vercel Deploy
        run: |
          curl --request POST \
          --url 'https://api.vercel.com/v1/integrations/deploy/${{ secrets.VERCEL_DEPLOY_ID }}'
